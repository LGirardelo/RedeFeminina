<p id="notice"><%= notice %></p>

<h1>Profissionals</h1>

<table>
  <thead>
    <tr>
      <th>Especializacao</th>
      <th>Pessoa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profissionals.each do |profissional| %>
      <tr>
        <td><%= profissional.especializacao %></td>
        <td><%= profissional.pessoa %></td>
        <td><%= link_to 'Show', profissional %></td>
        <td><%= link_to 'Edit', edit_profissional_path(profissional) %></td>
        <td><%= link_to 'Destroy', profissional, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profissional', new_profissional_path %>
