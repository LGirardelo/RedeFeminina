<%= form_for @tipocancer, :html => { :class => "form-horizontal tipocancer" } do |f| %>

  <% if @tipocancer.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tipocancer.errors.count, "error") %> prohibited this tipocancer from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tipocancer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :descricao, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :descricao, :class => 'form-control' %>
    </div>
    <%=f.error_span(:descricao) %>
  </div>
  <div class="form-group">
    <%= f.label :nivelgravidade, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nivelgravidade, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nivelgravidade) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tipocancers_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
